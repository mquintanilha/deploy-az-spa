on:
  push:
    branches:
      - main
    
  pull_request:
    branches:
      - "*"

jobs:
  init:
    runs-on: ubuntu-latest
    steps:
      - id: Checkout repository
        uses: actions/checkout@v2

  build:
    runs-on: ubuntu-latest
    steps:
      - id: build
        run: |
          make