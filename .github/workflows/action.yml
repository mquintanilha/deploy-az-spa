on:
  push:
    branches:
      - main
    
  pull_request:
    branches:
      - "*"

jobs:
  init:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@develop

  build:
    runs-on: ubuntu-latest
    steps:
      - name: build
        run: |
          echo $PWD
          ls -la $PWD